AUDD_TOKEN=your_audd_api_token_here
SAMPLE_RATE=44100
LYRIC_OFFSET=2.0

# AUDIO_DEVICE=15   # bare-metal only: device index from `python -c "import sounddevice; print(sounddevice.query_devices())"`
#                   # leave unset when running via Docker (PulseAudio default is used)
